name: Run Tests

# Manual test execution only - automatic tests are handled by:
# - docker-build.yml (on push to master or tags v*)
# - pr-gate.yml (on pull requests to master)
on:
  pull_request:
    branches:
      - master
    types: [labeled]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'run-tests'))

    steps:
      - uses: actions/checkout@v4

      - name: Create .env file
        run: printf 'MYSQL_USER=root\nMYSQL_PASSWORD=root\nMYSQL_HOST=db\n' > .env

      - name: Configure bundler to use vendor/bundle
        run: |
          mkdir -p .bundle
          printf 'BUNDLE_PATH: "vendor/bundle"\nBUNDLE_JOBS: "4"\nBUNDLE_RETRY: "3"\n' > .bundle/config

      - name: Cache gems
        uses: actions/cache@v4
        with:
          path: vendor/bundle
          key: ${{ runner.os }}-gems-${{ hashFiles('Gemfile.lock') }}
          restore-keys: ${{ runner.os }}-gems-

      - name: Start database
        run: docker compose up -d db

      - name: Build web image
        run: docker compose build web

      - name: Install gems
        run: docker compose run --rm web bundle install

      - name: Wait for database
        run: |
          until docker compose exec -T db mysqladmin ping -uroot -proot --silent; do
            sleep 2
          done

      - name: Setup test database
        run: docker compose run --rm web bin/rails db:create db:schema:load RAILS_ENV=test

      - name: Run tests
        run: docker compose run --rm web bin/rails test

      - name: Upload test artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: log/test.log

  cleanup:
    runs-on: ubuntu-latest
    needs: [test]
    if: always() && github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'run-tests')
    permissions:
      pull-requests: write

    steps:
      - name: Remove run-tests label
        uses: actions/github-script@v7
        with:
          script: |
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                name: 'run-tests'
              });
            } catch (error) {
              if (error.status !== 404) throw error;
            }
